jdk:
  - openjdk9
before_install:
  - apt-get install libatomic1
install:
  - echo "Running custom install"
  - ./gradlew clean publish -xtest
  - echo "Copy published repo"
  - cp build/repository ~/.m2/repository